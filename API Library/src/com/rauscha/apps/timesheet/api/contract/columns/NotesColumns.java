package com.rauscha.apps.timesheet.api.contract.columns;

public interface NotesColumns {
	/** Note UUID. */
    String NOTE_ID = "note_uuid";
    /** Note text. */
    String NOTE_TEXT = "note_text";
    /** Creation time of note. */
    String NOTE_DATE = "note_date";
    /** Note attachment. */
    String NOTE_URI = "note_uri";
}